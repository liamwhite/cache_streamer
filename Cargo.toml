[package]
name = "cache_streamer"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
cache_streamer_http = { path = "libs/cache_streamer_http" }
env_logger = "0.11"
tokio = { version = "1", features = ["full"] }
